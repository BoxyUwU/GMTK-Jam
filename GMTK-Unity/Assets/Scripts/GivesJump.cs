using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GivesJump : MonoBehaviour
{
    // This is basically just a bool that we attach to the ground to know through code whether colliding
    // with the object gives us our jump back

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
